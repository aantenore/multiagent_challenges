## Task
Analyse the following **{role}** data for entity '{entity_id}' and decide whether the trajectory suggests *preventive support* (1) or *standard monitoring* (0).

{semantic_section}
### {role_title} Data
```json
{data_slice}
```

### Profile Summary
```json
{profile_json}
```

### Context
{context}

### Ground Truth Reference (RAG)
{rag_section}

### Instructions
- Look for deteriorating trends, anomalies, or warning signals.
- **Reference Thresholds**:
    - PhysicalActivityIndex: < 0.3 (Sedentary/Atrophy Risk), 0.3-0.7 (Standard), > 0.7 (Active).
    - SleepQualityIndex: < 0.4 (Chronic Fatigue/Disorder), > 0.8 (Optimal).
    - EnvironmentalExposureLevel: > 0.7 (Toxic/Hazardous Exposure).
- A sharp DROP in PhysicalActivityIndex or SleepQualityIndex across events is a red flag.
- EventTypes like 'emergency visit', 'specialist consultation', 'follow-up assessment' signal escalation.
- Reduced mobility radius or sudden isolation patterns indicate withdrawal.
- **RAG Authority**: Historical cases with label=0 are confirmed stable profiles. Use them to filter out similar trajectories that might look like anomalies but are actually typical for this cohort.

Respond with JSON: {{"prediction": 0 or 1, "confidence": 0.0-1.0, "reasoning": "..."}}
