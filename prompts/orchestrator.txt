## Global Decision for Entity '{entity_id}'

### Swarm Consensus Verdicts
{verdict_lines}

### Entity Profile
```json
{profile_json}
```

### Context
{context}

### Engineered Features
```json
{features_json}
```

{rag_section}
### Economic Framework
- False Positive cost:  {fp_cost} units (wasted resources)
- False Negative cost:  {fn_cost} units (irreversible harm)
- FN is {fn_ratio:.0f}× worse than FP.

### Instructions
1. Each verdict above comes from a **Role Coordinator** that ran multiple expert agents. Pay attention to:
   - **Agreement ratio** — how many agents within each role agree (higher = stronger signal).
   - **Confidence** — the consensus confidence for that role.
   - **Complexity** — higher complexity means the case is harder; weight these roles more carefully.
2. Consider the asymmetric cost: err on the side of flagging (prediction=1) when uncertain.
3. Look for cross-role consensus — if multiple roles flag concerns, the evidence is very strong.
4. Even ONE strong signal (e.g., emergency visit, specialist consultation, sharp metric decline) should bias toward prediction=1.
5. A role with low agreement but high complexity suggests genuine ambiguity — apply extra caution.
6. Provide your final prediction.

Respond with JSON: {"prediction": 0 or 1, "confidence": 0.0-1.0, "reasoning": "..."}
