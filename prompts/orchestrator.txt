## Global Decision for Entity '{entity_id}'

### Swarm Consensus Verdicts
{verdict_lines}

### Entity Profile
{profile_summary}

### Key Engineered Features (Significant Deviations)
```json
{features_summary}
```

{rag_section}
### Economic Framework
- False Positive cost:  {fp_cost} units (wasted resources)
- False Negative cost:  {fn_cost} units (irreversible harm)
- FN is {fn_ratio:.0f}× worse than FP.

### Instructions
1. Each verdict above comes from a **Role Coordinator** that ran a swarm of expert agents for specific data roles defined in the manifest.
2. **Direct Contextual Analysis**: Use the `Entity Profile` and `Context` (notes/personas) to determine if the findings from the domain swarms are justified by the entity's history or environment.
3. **Reconciliation**:
   - If L1 signals an anomaly but Profile/Context justifies it → Predict 0.
   - If L1 is uncertain (low agreement/confidence) but Profile/Context shows high risk → Predict 1.
4. **Economic Decision**: FN is {fn_ratio:.0f}× worse than FP. Err on the side of flagging (prediction=1) if the profile doesn't clearly explain away the detected deviations.
5. **RAG Authority**: Use historical cases to benchmark what "Normal" or "Tricky" looks like for this demographic.

Respond with JSON: {{"prediction": 0 or 1, "confidence": 0.0-1.0, "reasoning": "Detailed breakdown of why Profile/Context justifies or confirms the domain swarm signals."}}
